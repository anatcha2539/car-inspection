# 🚗 ระบบจัดการและตรวจเช็คสภาพรถยนต์ (Vehicle Inspection System)

เว็บแอปพลิเคชันสำหรับบริหารจัดการยานพาหนะ ตรวจเช็คสภาพรถประจำวัน และแจ้งซ่อมบำรุง พัฒนาด้วย **Django Framework** ช่วยเปลี่ยนกระบวนการจากกระดาษสู่ระบบดิจิทัล (Paperless)

## ✨ ฟีเจอร์หลัก (Key Features)

### 👨‍✈️ สำหรับพนักงานขับรถ (Driver)
* **ระบบล็อกอิน:** เข้าสู่ระบบเพื่อดูงานของตัวเอง
* **ตารางงาน (To-Do List):** ดูรายการรถที่ได้รับมอบหมายให้ตรวจในแต่ละวัน (Daily Schedule)
* **แบบฟอร์มตรวจเช็ค:** เช็คลิสต์ดิจิทัลครอบคลุมทุกระบบ (เครื่องยนต์, ไฟ, ช่วงล่าง ฯลฯ)
* **แจ้งซ่อม:** รายงานปัญหาที่พบพร้อมรายละเอียด
* **ประวัติ:** ดูประวัติการตรวจย้อนหลังของตัวเอง

### 👮‍♂️ สำหรับผู้ดูแลระบบ (Admin/Manager)
* **Dashboard สรุปผล:** กราฟแสดงสถิติการตรวจ, สถานะรถ, และงานซ่อมบำรุง (Chart.js)
* **ระบบจัดตารางเวร:** มอบหมายงานตรวจรถให้พนักงานเป็นรายวัน
* **แจ้งเตือนอัจฉริยะ:**
    * 🔔 แจ้งเตือนเมื่อมีการแจ้งซ่อมใหม่
    * ⚙️ **แจ้งเตือนอัตโนมัติ** เมื่อเลขไมล์รถถึงระยะที่ต้องเข้าศูนย์บริการ
* **จัดการข้อมูล:** เพิ่ม/ลบ/แก้ไข ข้อมูลรถและพนักงาน
* **รายงาน PDF:** พิมพ์ใบตรวจเช็คสภาพรถเป็นไฟล์ PDF ขนาด A4 เพื่อใช้เป็นเอกสารหลักฐาน

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)
* **Backend:** Python 3.x, Django 5.x
* **Database:** SQLite (Default)
* **Frontend:** HTML5, CSS3, Bootstrap 5
* **Charts:** Chart.js
* **PDF Generation:** Browser Print-to-PDF Layout

## ⚙️ การติดตั้งและรันโปรเจค (Installation)

1.  **Clone หรือ ดาวน์โหลดโปรเจค**
    ```bash
    git clone <your-repo-url>
    cd car-web-app
    ```

2.  **สร้าง Virtual Environment**
    ```bash
    # สำหรับ Windows
    python -m venv venv
    .\venv\Scripts\activate
    ```

3.  **ติดตั้ง Library ที่จำเป็น**
    ```bash
    pip install -r requirements.txt
    ```

4.  **ตั้งค่าฐานข้อมูล (Database)**
    ```bash
    python manage.py makemigrations
    python manage.py migrate
    ```

5.  **สร้างบัญชีผู้ดูแลระบบ (Superuser)**
    ```bash
    python manage.py createsuperuser
    # กรอก Username, Email, Password ตามต้องการ
    ```

6.  **รันเซิร์ฟเวอร์ (Run Server)**
    ```bash
    python manage.py runserver
    ```

## 📖 คู่มือการใช้งานเบื้องต้น (Usage)

### การเข้าใช้งาน
* **หน้าแรก/Login:** `http://127.0.0.1:8000/`
* **หน้า Admin:** `http://127.0.0.1:8000/admin/`
* **หน้า Dashboard ผู้บริหาร:** `http://127.0.0.1:8000/dashboard-admin/`

### Workflow การทำงาน
1.  **Admin** เข้าสู่ระบบหลังบ้าน เพื่อเพิ่มข้อมูล "รถ" และ "พนักงาน"
2.  **Admin** สร้าง "ตารางเวร" (Schedule) มอบหมายรถให้พนักงานในวันนั้น
3.  **Driver** ล็อกอินเข้าระบบ จะเห็นรายการรถที่ต้องตรวจ
4.  **Driver** ทำการตรวจเช็คและบันทึกข้อมูล (หรือแจ้งปัญหา)
5.  หากรถถึงระยะถ่ายน้ำมันเครื่อง ระบบจะสร้าง Ticket แจ้งเตือน Admin อัตโนมัติ
6.  **Admin** ดูสรุปผลที่ Dashboard หรือพิมพ์รายงาน PDF เก็บไว้

## 📁 โครงสร้างโปรเจค (Project Structure)

```text
car-web-app/
├── car_project/            # การตั้งค่าหลักของโปรเจค (Settings, URLs)
├── inspection/             # แอปพลิเคชันหลัก
│   ├── models.py           # ฐานข้อมูล (Vehicle, Inspection, Schedule)
│   ├── views.py            # Logic การทำงาน
│   ├── forms.py            # แบบฟอร์ม
│   ├── admin.py            # การตั้งค่าหน้า Admin
│   └── templates/          # ไฟล์ HTML
├── templates/              # ไฟล์ Template กลาง (base.html)
├── static/                 # ไฟล์ CSS, JS, Images
└── manage.py               # ตัวสั่งการ Django